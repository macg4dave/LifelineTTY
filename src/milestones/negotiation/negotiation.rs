//! Negotiation payload models. This placeholder will document the capability
//! bitmaps, handshake timeouts, and fallback rules before the real serde structs exist.

/// Describes the capability frame that will be sent during auto-negotiation.
pub struct CapabilityFrame;

impl CapabilityFrame {
    /// Placeholder builder that will eventually encode the capability bitmap.
    pub fn builder() -> Self {
        Self
    }
}
