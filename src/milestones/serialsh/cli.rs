//! CLI helper stub for the serialsh mode. Will add `--serialsh` parsing, history handling, and gating against service mode.

/// Placeholder for flags and configuration pertinent to serialsh.
pub struct SerialShCli;

impl SerialShCli {
    /// Placeholder method documenting how the interactive flag will be detected.
    pub fn is_enabled(&self) -> bool {
        false
    }
}
